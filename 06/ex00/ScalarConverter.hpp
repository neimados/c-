#pragma once

#include <iostream>

class ScalarConverter {
    public:
        // ScalarConverter();
        // ScalarConverter(const ScalarConverter &conv);
        // ScalarConverter &operator=(const ScalarConverter &conv);
        // ~ScalarConverter();

        // void    convert(const char *argv);

    private:
        char    _c;
        int     _i;
        float   _f;
        double  _d;
};
